<!--
    
    Search box used in the home page and on the search results page.
-->
    
    
<UserControl
    x:Class="Tempo.SearchBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tempo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <TextBox x:Name="_searchBox" KeyDown="_searchBox_KeyDown"
                     InputScope="Search" IsSpellCheckEnabled="False"
                     Text="{x:Bind local:App.Instance.SearchText, Mode=TwoWay}"
                     PlaceholderText="{x:Bind local:MainPage.PlaceholderText}"/>

        <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Button Click="Search_Click" >
                <SymbolIcon Symbol="Find"  />
            </Button>
            <Button Click="ShowAll" Visibility="{x:Bind IsAllVisible, Mode=OneWay}">Show everything</Button>
            <Button Click="ShowFilters">
                Filters
                <Button.KeyboardAccelerators>
                    <KeyboardAccelerator Modifiers="Control" Key="F"/>
                    
                    <!-- Keeping the old one around to see how it looks -->
                    <KeyboardAccelerator Modifiers="Control,Shift" Key="F"/>
                </Button.KeyboardAccelerators>
            </Button>
        </StackPanel>
    </Grid>
    
</UserControl>
