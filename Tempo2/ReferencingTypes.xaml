<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<local:MySerializableControl
    x:Class="Tempo.ReferencingTypes"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tempo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto,*">
        <TextBlock Margin="0,10,0,10">Types referencing <Run Text="{x:Bind ReferencedType.PrettyFullName}"/></TextBlock>

        <Grid Grid.Row="1" ColumnDefinitions="Auto,*" >
            <ListView ItemsSource="{x:Bind Types, Mode=OneWay}"
                      SelectionChanged="ListView_SelectionChanged"
                      SelectedItem="{x:Bind SelectedItem, Mode=TwoWay}"/>
            
            <ListView Grid.Column="1" ItemsSource="{x:Bind Routes, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate >
                        <ItemsControl ItemsSource="{Binding}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</local:MySerializableControl>
