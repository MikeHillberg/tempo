<Page
    x:Class="Tempo.TypeOrMemberDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tempo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="White"  Padding="10"  x:Name="_detailsGrid">
        <local:TypeDetailPage  Background="White" x:Name="_typeDetail" IsTopLevel="{x:Bind IsTopLevel, Mode=OneWay}" />
        <local:MethodDetailView Background="White" x:Name="_methodDetail" IsTopLevel="{x:Bind IsTopLevel, Mode=OneWay}" />
        <local:EventDetailView Background="White" x:Name="_eventDetail" IsTopLevel="{x:Bind IsTopLevel, Mode=OneWay}" />
        <local:ConstructorDetailView Background="White" x:Name="_constructorDetail" IsTopLevel="{x:Bind IsTopLevel, Mode=OneWay}" />
        <local:FieldDetailView Background="White" x:Name="_fieldDetail" IsTopLevel="{x:Bind IsTopLevel, Mode=OneWay}"/>
        <local:PropertyDetailView Background="White" x:Name="_propertyDetail" IsTopLevel="{x:Bind IsTopLevel, Mode=OneWay}" />
    </Grid>
</Page>
