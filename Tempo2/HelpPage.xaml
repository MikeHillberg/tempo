
<Page
    x:Class="Tempo.HelpPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tempo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Background="Transparent"
    RequestedTheme="{x:Bind local:App.Instance.ElementTheme, Mode=OneWay}">

    <ScrollViewer Margin="50" MaxWidth="800" 
                  Background="{ThemeResource SystemControlBackgroundAltHighBrush}"
                  HorizontalAlignment="Center">

        <!-- Use the community toolkit's markdown reader -->
        <!-- Help text is in help.md -->
        <controls:MarkdownTextBlock x:Name="_markdown" Background="Transparent" Padding="50"/>
    </ScrollViewer>
    
</Page>
