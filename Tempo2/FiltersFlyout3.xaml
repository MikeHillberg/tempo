<Flyout
    x:Class="Tempo.FiltersFlyout3"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tempo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Flyout.FlyoutPresenterStyle>
        
        <!-- Need some overrides to get the sizing right -->
        <Style TargetType="FlyoutPresenter">
            <Setter Property="MinHeight" Value="400"/>

            <!-- Have to set both Width and MaxWidth to get it to stick -->
            <Setter Property="Width" Value="600"/>
            <Setter Property="MaxWidth" Value="600"/>
        </Style>
    </Flyout.FlyoutPresenterStyle>

    <!-- The Width needs to match the Width above -->
    <local:Filters3 Width="600"  />

</Flyout>
